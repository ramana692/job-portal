{
  "buildCommand": "cd jobportfolio-frontend && CI=false npm install && CI=false npm run build",
  "outputDirectory": "jobportfolio-frontend/build",
  "installCommand": "cd jobportfolio-frontend && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
